# list
alias l='ls -l'
alias ll='ls -l'
alias la='ls -la'

#bundle exec
alias berake='bundle exec rake'
alias berspec='bundle exec rspec'
alias berails='bundle exec rails'

# others
alias g='git'
alias h='hub'

# integra
alias erspec='SCREENSHOT=true berspec'
